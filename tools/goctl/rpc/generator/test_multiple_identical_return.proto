// test proto
syntax = "proto3";

package test;

message TestReq{}
message TestReply{
}

enum TestEnum {
  unknown = 0;
  male = 1;
  female = 2;
}

service TestService{
  rpc TestRpc (TestReq)returns(TestReply);
  rpc TestRpc2 (TestReq)returns(TestReply);
}